apiVersion: v1
kind: Service
metadata:
  name: {{ include "ishop-authorization.fullname" . }}
  labels:
    {{- include "ishop-authorization.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ishop-authorization.selectorLabels" . | nindent 4 }}
