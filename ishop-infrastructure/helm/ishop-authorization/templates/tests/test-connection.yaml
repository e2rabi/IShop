apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ishop-authorization.fullname" . }}-test-connection"
  labels:
    {{- include "ishop-authorization.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ishop-authorization.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
